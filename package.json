{
  "name": "claude-usage-pace",
  "version": "4.2.1",
  "private": true,
  "description": "Tampermonkey userscript that overlays pace indicators on the claude.ai usage panel (#settings/usage)",
  "author": "rad-orlowski",
  "type": "module",
  "scripts": {
    "postinstall": "bun install --cwd src/mcp",
    "build": "bun run build.ts && bun run --cwd src/mcp build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^25.9.1",
    "happy-dom": "^20.9.0"
  }
}